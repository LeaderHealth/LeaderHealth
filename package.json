{
  "name": "leader-health",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "concurrently -n web,storefront -c cyan,magenta \"next dev\" \"npm run dev --prefix storefront\"",
    "dev:web": "next dev",
    "dev:storefront": "npm run dev --prefix storefront",
    "build:storefront": "npm run build --prefix storefront && node scripts/copy-storefront.mjs",
    "build": "npm install --prefix storefront && npm run build:storefront && next build",
    "start": "next start",
    "lint": "eslint"
  },
  "dependencies": {
    "framer-motion": "^13.2.0",
    "next": "16.3.5",
    "react": "19.2.8",
    "react-dom": "19.2.8"
  },
  "devDependencies": {
    "@tailwindcss/postcss": "^4",
    "@types/node": "^20",
    "@types/react": "^19",
    "@types/react-dom": "^19",
    "eslint": "^9",
    "concurrently": "^9.2.1",
    "eslint-config-next": "16.3.5",
    "tailwindcss": "^4",
    "typescript": "^5"
  }
}
